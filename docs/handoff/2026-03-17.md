# school_sync (StudyHub) Handoff — 2026-03-17

## 현재 상태
전체 코드 심층 감사 + P0~P3 이슈 해결 + 구조적 리팩토링 완료. VPS 배포 대기 중.

## 작업 내용

### 1차 리팩토링 (이전 이슈 해결)
- P2 3건 해결: HTML fallback 셀렉터, "강의" 2차 분류, TimetableEntry/SyllabusEntry 모델 적용
- P3 7건 해결: browser.py 컨텍스트 매니저, cache.py 원자적 쓰기/데드코드, GradeItem.score_range, NormalizedOutput 통합, --refresh 도움말, syllabus 품질
- 웹 리팩토링 6건: sys.path 제거(ask_engine.py), useLogStream 훅, auto_sync 반응성, ErrorBoundary, tasks.py 멀티워커, docker-compose 리소스 제한
- SyOps nginx study.conf 추가

### 심층 감사 (P0/P1 보안 수정)
- **P0 5건**: ask 세션 격리, 파일 업로드 DoS 방어, course_filter 플래그 주입 차단, PII 마스킹, useLogStream 무한루프
- **P1 11건**: DEV_MODE 경고, 에러 정보 차단, HTTP 상태코드, 과목 정확 매칭, main.py 경로, 캘린더 타임존, 히스토리 corruption, ask_engine fallback, deprecated API, Anthropic 싱글턴

### 구조적 리팩토링 (P2-P3)
- `utils.py` 공통 모듈 (save_json, load_json, setup_win_encoding)
- `GOTO_TIMEOUT_MS` 상수 통합 (6곳 중복 제거)
- `CacheBatch` context manager (O(n²) → O(n))
- data_loader mtime 캐시
- normalizer.py 분리 → normalizer_storage.py + briefing.py
- NormalizedOutput.grade_history 모델 통합
- config.py lazy loading (_ensure_loaded)
- CORS 범위 축소, GDrive 토큰 원자적 쓰기, tasks.py 참조 보관
- health 정보 분리, portal dedup, 크롤러 에러 구조(CrawlError), auto_sync 시간 드리프트

## 핵심 결정 & 맥락
- normalizer.py를 3개 모듈로 분리하되, 순환 import 방지를 위해 normalize() 함수 내 lazy import 사용
- config.py lazy loading: 모듈 레벨 변수 유지 (`from config import X` 패턴 호환), 초기화만 _ensure_loaded()로 감쌈
- CrawlError dataclass는 정의만 하고, 기존 크롤러에는 점진적으로 적용 예정
- PII 마스킹은 모델에서 제거하지 않고 LLM 전송 시점에서만 필터링 (정규화 데이터 완전성 유지)

## 다음 작업
- [ ] VPS 배포: `cd /opt/apps/school-sync && git pull && docker compose up -d --build`
- [ ] SyOps VPS 배포: `cd /opt/apps/SyOps && git pull` → nginx study.conf 적용 확인
- [ ] 프론트엔드 빌드 확인 (ErrorBoundary, useLogStream 적용)
- [ ] (선택) CrawlError를 실제 크롤러에 점진 적용

## 세션 컨텍스트
- **대화 주제**: StudyHub 전체 개선 — 상황 파악 → 이슈 해결 → 심층 감사 → P0-P1 수정 → P2-P3 리팩토링
- **주요 논의 흐름**: 3개 독립 감사 에이전트로 백엔드/코어/프론트엔드 동시 감사 → 우선순위별 일괄 수정 → 재감사로 CRITICAL 2건 추가 발견/수정
- **마지막 작업**: 전체 커밋 + 푸시 완료
- **참고**: 감사에서 CRITICAL로 발견된 _GOTO_TIMEOUT 교체 누락은 portal.py와 department.py의 메서드 내부에서 발생 — replace_all 사용 시에도 문자열 매칭 범위에 주의 필요

The Record 갱신: ✅ (별도 갱신 필요 시 워크스페이스 모드에서 진행)
